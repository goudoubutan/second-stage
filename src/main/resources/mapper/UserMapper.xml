<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.csqf.zx.mapper.UserMapper">
    <resultMap type="com.csqf.zx.entity.UserEntity" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="rid" column="rid" jdbcType="INTEGER"/>
    </resultMap>

    <!--通过手机号查询用户-->
    <select id="queryUserByPhone" resultMap="UserMap">
        select
        id, username, password, phone, email, rid,money
        from user
        where phone = #{phone}
    </select>

    <!--通过微信openId查询用户-->
    <select id="findByOpenId" resultMap="UserMap">
        select
            id, username, password, phone, email, rid,money
        from user
        where openid = #{openId}
    </select>


    <!--新增所有列-->
    <insert id="register" keyProperty="id" useGeneratedKeys="true">
        insert into user(username, password, phone, email, rid,money)
        values (#{registerVO.username}, #{registerVO.password}, #{registerVO.phone}, #{registerVO.email}, #{registerVO.rid},#{registerVO.money})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user(username, password, phone, email, rid)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.phone}, #{entity.email}, #{entity.rid})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user(username, password, phone, email, rid)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.phone}, #{entity.email}, #{entity.rid})
        </foreach>
        on duplicate key update
        username = values(username),
        password = values(password),
        phone = values(phone),
        email = values(email),
        rid = values(rid)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="rid != null">
                rid = #{rid},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from user where id = #{id}
    </delete>
</mapper>